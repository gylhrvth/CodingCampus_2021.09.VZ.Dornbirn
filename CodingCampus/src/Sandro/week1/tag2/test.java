package Sandro.week1.tag2;

public class test {
    public static void main(String[] args) {

        char[] name  = { 'C', 'h', 'r', 'i', 's' };
        char   first = name[ 0 ];                       // C
        char   last  = name[ name.length -1];
        System.out.println(first + last);
    }
}
