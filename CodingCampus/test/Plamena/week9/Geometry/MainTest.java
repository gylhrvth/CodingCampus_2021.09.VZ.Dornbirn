package Plamena.week9.Geometry;
/*
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

import java.util.LinkedList;
import java.util.List;

public class MainTest {

    private static List<Geometry> geometricFigures;

    @BeforeAll
    public static void createGeometricFiguresList() throws Exception {
        geometricFigures = new LinkedList<>();
        geometricFigures.add(new Triangle(1.0, 2.0, 2.0));
        geometricFigures.add(new Square(2.0));
        geometricFigures.add(new Circle(5.0));
    }

    @Test
    public void getCircumferenceOverall() {
        Assertions.assertEquals(45.416, Main.getCircumferenceOverall(geometricFigures), 0.002);
    }

    @Test
    public void getAreaOverall(){
        Assertions.assertEquals(83.5, Main.getAreaOverall(geometricFigures), 0.2);
    }
}


 */